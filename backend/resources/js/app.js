import './bootstrap.js';

/**
 * First, we will load all of this project's JavaScript dependencies which
 * includes Vue and other libraries. It is a great starting point when
 * building robust, powerful web applications using Vue and Laravel.
 */

require('./bootstrap.js');

/**
 * Next, you could add your custom JavaScript, or if you are using a JavaScript
 * framework like Vue.js, you might start instantiating your application or components here.
 */

// Beispiel für benutzerdefinierten JavaScript-Code
// document.getElementById("demo").innerHTML = "Hello JavaScript!";

// Beispiel für die Initialisierung von Vue.js (wenn Vue installiert ist)
// const app = new Vue({
//     el: '#app',
// });
